<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="ArknightsLauncher.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="设置"
    PrimaryButtonText="保存"
    CloseButtonText="取消"
    DefaultButton="Primary">

    <StackPanel Spacing="12" MinWidth="380">

        <!-- 游戏目录 -->
        <TextBlock Text="游戏目录" FontWeight="SemiBold"
                   Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="8" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="GamePathText"
                           FontSize="12"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
                <Button Grid.Column="1" Margin="8,0,0,0"
                        Padding="12,6"
                        Click="BrowseGameBtn_Click">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8B7;" FontSize="12"/>
                        <TextBlock Text="浏览" FontSize="12"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- MAA 目录 -->
        <TextBlock Text="MAA 路径（可选）" FontWeight="SemiBold"
                   Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="8" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="MAAPathText"
                           FontSize="12"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
                <Button Grid.Column="1" Margin="8,0,0,0"
                        Padding="12,6"
                        Click="BrowseMAABtn_Click">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8B7;" FontSize="12"/>
                        <TextBlock Text="浏览" FontSize="12"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- 主题 -->
        <TextBlock Text="主题" FontWeight="SemiBold"
                   Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
        <StackPanel Orientation="Horizontal" Spacing="8">
            <RadioButton x:Name="ThemeLight" Content="浅色" GroupName="Theme"/>
            <RadioButton x:Name="ThemeDark"  Content="深色" GroupName="Theme"/>
            <RadioButton x:Name="ThemeSystem" Content="跟随系统" GroupName="Theme"/>
        </StackPanel>

        <!-- 窗口材质 -->
        <TextBlock Text="窗口材质" FontWeight="SemiBold"
                   Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
        <ComboBox x:Name="MaterialCombo" HorizontalAlignment="Stretch">
            <ComboBoxItem Content="无"/>
            <ComboBoxItem Content="亚克力"/>
            <ComboBoxItem Content="云母"/>
            <ComboBoxItem Content="云母Alt"/>
        </ComboBox>

        <!-- 语言 -->
        <TextBlock Text="语言" FontWeight="SemiBold"
                   Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
        <StackPanel Orientation="Horizontal" Spacing="8">
            <RadioButton x:Name="LangCN" Content="简体中文" GroupName="Lang"/>
            <RadioButton x:Name="LangEN" Content="English"
                         GroupName="Lang" IsEnabled="False" Opacity="0.4"/>
        </StackPanel>

    </StackPanel>
</ContentDialog>